<%= form_for @product, url: admin_product_path(@product) do |f| %>
  <div>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.text_area :description %>
  </div>
  <div>
    <%= f.file_field :image %>
  </div>
  <%= f.submit %>
<% end %>
